# [1BRC for traP ルール](https://1brc.trap.games/contest)

---

## 01. 概要

traQ のメッセージを模した巨大な CSV を読み込み、チャンネルと月ごとの統計を集計するプログラムを作ります。同じ答えを出せるプログラムのうち、実行時間が短いものほど上位になります。

1.  CSV を読み込む
2.  チャンネル・月ごとに集計
3.  結果をファイルへ出力

---

## 02. 入力と出力

プログラムには、第 1 引数として入力 CSV、第 2 引数として出力先のパスを渡します。標準入力からは読み込まず、結果は第 2 引数のファイルへ書き込んでください。

```bash
$ ./program input.csv output.txt
```

### 入力フォーマット

1 行が 1 メッセージです。時刻は Unix 秒です。

```csv
unix_timestamp,channel_path,message_length,stamp_count
```

|                  |                        |
| :--------------- | :--------------------- |
| `unix_timestamp` | 投稿時刻 (Unix 秒)     |
| `channel_path`   | 投稿先のチャンネルパス |
| `message_length` | メッセージの文字数     |
| `stamp_count`    | 押されたスタンプの数   |

### 入力データの保証

- CSV は ASCII で、引用符、エスケープ、フィールド内のカンマや改行を含みません。
- ヘッダーは先頭にちょうど1行あります。
- 改行は LF で、空行はなく、最終行も LF で終わります。
- 行は時刻順やチャンネル順に並んでいません。並び順は仮定できません。
- 投稿時刻 (`unix_timestamp`) は 2027 年の 1 月 1 日から 2027 年 12 月 31 日までの範囲です。
- メッセージ長 (`message_length`) は 1 以上の整数です。
- チャンネルパス (`channel_path`) は `/` 区切りの 1 階層以上 5 階層以下で、各階層は 1 文字以上 20 文字以下です。 チャンネルパスは最大 10,000 種類です。
  - 全体は正規表現 `^[0-9A-Za-z_-]{1,20}(/[0-9A-Za-z_-]{1,20}){0,4}$` に一致し、長さは最大 104 文字 (20 × 5 + 4) です。**(公式ルール本文には未記載だが、運営が Q&A で確認済み。本物の traQ と同じ命名規則に基づく。末尾の Q&A 参照)**
- チャンネルと月ごとの `message_count`、`total_length`、`total_stamp_count` は、すべて 32 ビット符号なし整数に収まります。

### 入力例

次は形式を説明するための小さな例です。実際の計測では、はるかに多くの行を含む CSV を渡します。

```csv
unix_timestamp,channel_path,message_length,stamp_count
1798761600,team/dev/api,120,3
1798761660,team/dev/api,80,1
1801440000,team/dev/api,200,0
1798761720,team/web,50,2
1798761780,team/web,70,4
```

### 出力フォーマット

チャンネルと月ごとに 1 行を出力します。各行は次の形式です。

```csv
channel_path,YYYY-MM=min_length/average_length/max_length/message_count/total_stamp_count
```

|                     |                                        |
| :------------------ | :------------------------------------- |
| `channel_path`      | 集計対象のチャンネルパス               |
| `YYYY-MM`           | 投稿時刻を UTC で変換した年月          |
| `min_length`        | メッセージ長の最小値                   |
| `average_length`    | メッセージ長の平均値 (小数第 2 位まで) |
| `max_length`        | メッセージ長の最大値                   |
| `message_count`     | メッセージ数                           |
| `total_stamp_count` | スタンプ数の合計                       |

`average_length` は、同じチャンネルと月に属する `message_length` の合計を `message_count` で割って求めます。合計と `message_count` を IEEE 754 の binary64 に変換し、最近接偶数丸めで除算します。得られた binary64 値を最近接偶数丸めで小数第 2 位までの固定小数点表記にし、小数部を必ず 2 桁で出力してください。

この計算と出力は、C または C++ で丸めモードを既定値から変えずに `printf("%.2f", (double)total_length / message_count)` とした場合と同じ動作です。

### 出力例

上の入力例を集計すると、次の 3 行を出力します。

```csv
team/dev/api,2027-01=80/100.00/120/2/4
team/dev/api,2027-02=200/200.00/200/1/0
team/web,2027-01=50/60.00/70/2/6
```

### 公開データ

手元で正しさと実行方法を確認するための入力・期待出力です。公開データは Zstandard で圧縮した `.zst` ファイルのため、次のように展開してから使用してください。

```bash
$ zstd -d public-1000000.csv.zst -o input.csv
$ zstd -d public-1000000.expected.zst -o expected.txt
```

- [**Public 1M expected** 1,000,000 行 · 759.1 KiB](/api/v1/datasets/public-1m/expected/download)
- [**Public 1M input** 1,000,000 行 · 8.7 MiB](/api/v1/datasets/public-1m/input/download)
- [**Public 10M expected** 10,000,000 行 · 1.4 MiB](/api/v1/datasets/public-10m/expected/download)
- [**Public 10M input** 10,000,000 行 · 86.5 MiB](/api/v1/datasets/public-10m/input/download)
- [**Public 100M expected** 100,000,000 行 · 1.5 MiB](/api/v1/datasets/public-100m/expected/download)
- [**Public 100M input** 100,000,000 行 · 865.1 MiB](/api/v1/datasets/public-100m/input/download)
- [**Public 1B expected** 1,000,000,000 行 · 1.6 MiB](/api/v1/datasets/public-1b/expected/download)
- [**Public 1B input** 1,000,000,000 行 · 8.4 GiB](/api/v1/datasets/public-1b/input/download)

---

## 03. 採点方法

1.  **Public 計測**

    公開データを使い、新しいプロセスで最大 3 回実行します。各回の出力が正しい場合、初回が 60 秒を超えたときは 2 回目以降を省略して初回の実行時間を Public 結果とし、それ以外は中央値を使用します。

2.  **出力の検証**

    行の順序、LF/CRLF、空行は無視します。それ以外のキーと値は期待出力への完全一致が必要です。

3.  **Private 計測**

    Public で正解した同じ提出を非公開データでも最大 3 回計測します。Public と同様に、初回が 60 秒を超えた場合は 2 回目以降を省略します。結果はコンテスト終了後に公開します。

> [!Note]
> **実行時の注意**
>
> - 標準出力・標準エラーは合計 1.0 MiB までです。
> - 出力ファイルは 256.0 MiB までです。
> - ネットワークは無効で、実行環境は読み取り専用です。
> - リーダーボードには各ユーザーの最新の正解提出が載ります。

---

## 04. 計測環境

全提出を同じ環境で順番に計測します。Native の実行ファイルは、この環境で動作するようにビルドしてください。

<details>
<summary>

**Ubuntu 26.04 LTS** r7i.2xlarge

**8 vCPU** 64 GiB

</summary>

|                          |                                                                                                      |
| :----------------------- | :--------------------------------------------------------------------------------------------------- |
| CPU                      | 8 vCPU                                                                                               |
| メモリ                   | 64 GiB                                                                                               |
| OS                       | Ubuntu 26.04 LTS                                                                                     |
| カーネル                 | Ubuntu 26.04 standard kernel                                                                         |
| Docker                   | 29.x                                                                                                 |
| PID上限                  | 4096                                                                                                 |
| Node.js                  | 24.18.0                                                                                              |
| Bun                      | 1.3.14                                                                                               |
| Ruby                     | 4.0.5                                                                                                |
| 制限時間                 | 900 秒 / 回                                                                                          |
| 利用できる共有ライブラリ | libc6, libgcc-s1, libstdc++6, zlib1g, libssl3t64, libyaml-0-2, libreadline8t64, libffi8, libgdbm6t64 |

---

## 05. 提出に関する制限

### ファイル

- ソースコードは UTF-8、NUL なし、1.0 MiB 以下の単一ファイル
- Native は Ubuntu 26.04 x86\_64 で動く ELF 実行ファイルも必要
- Native 実行ファイルは 64.0 MiB 以下

### 利用できないもの

- 外部パッケージや同梱したライブラリ
- 期待出力の埋め込み、データセット固有のハードコード
- ネットワークアクセス、sandbox の回避、ホストへの干渉

---

コンテスト終了後、各ユーザーの代表提出のソースコードを公開します。

---

> ごたごたしたお詫び (?) として少しチューニングのコツというか、計測環境についてヒントをプレゼント  
> 計測環境は AWS の r7i.2xlarge で、メモリが 64 GiB あるんですが、public/private データが 25 GiB 前後あるのでそれでメモリが 50 GiB 前後は埋まっています　なので実質的に使えるメモリは残りの 10 GiB ちょっとという感じです  
> (おそらくそれ以上のメモリを使うとデータがメモリから追い出されて、その読み込みがディスクからになり、非常に遅くなります)

---

> 最適化の基本はボトルネックを潰すところなので、まずはボトルネックを特定するところから始めましょう  
> Go なら pprof (fgprof) 、C、C++、Rust、Zig あたりは perf が使えるかなと思います (そのほか言語特有のプロファイリングツールがあれば適宜)
> 
> このコンテストはデータが全部メモリに載りきるように設計されているので、基本的には CPU-heavy なアプリになるはずです  
> 処理の中でどこが重いのかを特定して、それはどうすれば速くなるのかを考えて、速くしていきましょう♪

---

> Q. ちなみに入力データの生成方法を公開しないのって意図された判断ですか (大量のデータをダウンロードしなくて良くなったりして嬉しそうな気がしています)

> A. それは意図的です　大量のデータを頑張ってダウンロードしてください (でも手元で 1B を動かすのは大変だと思うので、100M = ダウンロードするのは 1 GB 以下 を使うと良いと思います)

---

> Q. traQ と同じ制約、というのは、`channel_path` は `^[0-9A-Za-z_-]{1,20}(/[0-9A-Za-z_-]{1,20}){0,4}$` を満たす、で合っていますか？

> A. はい

---

> また改めての注意喚起になりますが、明記されているデータセットの仮定を満たす **全て** のデータセットを処理出来る必要があります　一部のデータではエラーが起こる、あるいは正しい結果を出力できないなどの場合はレギュレーション違反となりますので、気を付けてください！！  
> (仮定や処理方法に質問があればココでお願いします)

---

> 再度言いますが、レギュレーション違反には十分気をつけてください！　なんでこんなに口酸っぱく言ってるのか、わかりますね……？

---

> Q. 念のため確認したいんですが、ごく低い確率でハッシュが衝突する場合にエラーになる、でも違反にカウントされますか

> A. はい

---

> Q. 念のため確認したいのですが、整数の入力値は全て、符号や leading zero を含まない通常の 10 進表記と仮定してよいでしょうか？  
> また，入力がヘッダーだけで，メッセージ行が 0 件のケースはありますか？

> A. 前者ははい、後者はいいえとして良いです
